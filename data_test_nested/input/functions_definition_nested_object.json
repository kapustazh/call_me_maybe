[
  {
    "name": "fn_send_event",
    "description": "Send an analytics event with a nested JSON payload.",
    "parameters": {
      "event": { "type": "string" },
      "payload": { "type": "object" }
    },
    "returns": { "type": "string" }
  },
  {
    "name": "create_user",
    "description": "Create a user record from a nested JSON payload.",
    "parameters": {
      "attribute": { "type": "object" }
    },
    "returns": { "type": "string" }
  }
]
